# 全球科技活动聚合平台 - 产品需求文档 (PRD)

## 1. 项目概述

### 1.1 项目背景
全球科技活动（大会、研讨会、黑客松、Meetup 等）信息高度碎片化，分布在各种官方网站、社交媒体和票务平台。用户很难在一个地方查看到全球范围内的技术排期。本项目旨在构建一个**“全球科技活动索引”**，通过聚合各方数据，为用户提供一个上帝视角的活动时刻表。

### 1.2 项目目标
- **聚合与索引**：成为全球科技活动的第一入口。
- **消除时差阻碍**：提供智能时区转换，解决跨国参与的时间痛点。
- **精准导流**：作为流量枢纽，将用户快速引向活动官方页面。
- **日历集成**：支持将聚合的活动一键同步至用户个人日历。

### 1.3 核心原则 (Aggregation Principles)
- **不替代原站**：详情展示以摘要为主，深度内容（议程、演讲者）引导用户去原站阅读。
- **数据透明**：清晰标注数据来源，尊重原活动方的版权。
- **实时性**：重点在于时间的准确性和状态（进行中、已结束）的及时更新。

---

## 2. 核心功能需求

### 2.1 全球活动日历 (核心)

#### 2.1.1 视图模式
- **月视图**：宏观查看全球技术趋势和大型峰会排期。
- **周/日视图**：微观查看当天的具体活动排期，适合查看线上直播/会议。
- **列表视图**：最适合移动端，按时间顺序排列活动，支持无限滚动。

#### 2.1.2 关键交互
- **智能时区切换**：默认显示用户所在地时间，可一键切换至活动发生地时间（Local Time vs My Time）。
- **快速预览卡片**：在日历中点击活动，先弹出简要信息卡片（标题、时间、地点、摘要、来源），提供“查看预览”和“前往官网”两个选项。
- **颜色/分类标识**：根据技术领域（AI, Web3, Cloud 等）或形式（线上、线下）进行视觉区分。

### 2.2 聚合筛选系统

#### 2.2.1 筛选维度
- **活动形式**：纯线上直播、纯线下参会、混合模式。
- **地理位置**：按洲、国家、热门城市筛选（仅针对线下/混合活动）。
- **技术领域**：AI/ML, Web3, Frontend, Backend, DevOps, Security, Data Science, etc.
- **参与成本**：免费、付费（仅作为标签，不涉及交易）。
- **语言**：中文、英文、其他主要语言。

### 2.3 详情预览页 (Internal Preview Page)
*注：此页面作为“流量中转站”，旨在帮助用户快速决定是否值得跳转官网。*

#### 2.3.1 展示内容
- **活动标题与海报**：最直观的视觉识别。
- **核心摘要**：一句话简述活动目的。
- **时间信息**：开始/结束时间（多时区显示）、报名截止倒计时。
- **地点/链接预览**：线下详细地址（含地图预览）或线上平台入口。
- **数据来源标识**：明确显示“此活动聚合自 [主办方名称/原始平台]”。
- **免责声明**：提示用户“最终信息以官方页面为准”。

#### 2.3.2 核心动作 (Call to Action)
- **【前往官网】**：最醒目的主按钮，直接跳转至原活动官方页面。
- **【立即报名】**：如果能获取到直接报名链接，则提供快捷入口。
- **【添加到日历】**：支持 Google, Outlook, iCal 导出。
- **【分享/收藏】**：方便用户二次回访或传播。

---

## 3. 技术与数据需求

### 3.1 聚合活动模型
```typescript
interface TechEvent {
  id: string;
  title: string;
  summary: string;           // 简短摘要
  coverImage?: string;       // 活动海报/Logo
  startTime: Date;           // UTC 时间
  endTime: Date;
  timezone: string;          // 活动原始时区
  
  format: 'online' | 'offline' | 'hybrid';
  location?: {
    country: string;
    city: string;
    address?: string;
    coordinates?: { lat: number, lng: number };
  };

  tags: string[];            // 领域标签
  language: string[];        // 语言
  
  links: {
    officialSite: string;    // 活动官网 (必填)
    registration?: string;   // 直接报名链接
    source: string;          // 聚合来源 URL
  };

  organizer: {
    name: string;
    logo?: string;
  };

  price: {
    type: 'free' | 'paid' | 'unknown';
    range?: string;          // 例如 "$99 - $999"
  };

  status: 'upcoming' | 'live' | 'ended' | 'canceled';
}
```

### 3.2 聚合引擎要求 (Technical Backend)
- **去重算法**：基于标题相似度、时间重合度和地点的一致性，自动合并来自不同渠道的同一活动。
- **链接巡检**：定期自动检测 `officialSite` 的有效性，404 链接需自动标记或下线。
- **数据新鲜度**：关键活动信息（如取消、更名）需在 24 小时内完成同步更新。

---

## 4. 开发优先级

### 4.1 第一阶段：MVP (最小可行产品)
1. **基础日历**：月视图 + 列表视图，支持多时区切换。
2. **数据导入**：实现从主流科技活动源（如 Eventbrite, Luma, Devpost 等）的手动/半自动导入。
3. **预览卡片**：实现“快速预览 + 外部跳转”的核心链路。
4. **基础筛选**：按领域和线上/线下筛选。

### 4.2 第二阶段：用户增强
1. **日历订阅**：支持用户订阅特定筛选条件下的 iCal 流。
2. **搜索功能**：全文检索活动标题和摘要。
3. **移动端优化**：针对小屏幕优化列表视图体验。

### 4.3 第三阶段：社区与自动化
1. **用户提交**：开放入口允许用户提交活动，建立审核后台。
2. **完全自动化聚合**：构建成熟的爬虫集群和数据清洗管线。
3. **邮件周报**：根据用户关注的领域自动发送下周活动预告。

---

## 5. 待确认问题
- [ ] 初始数据源：优先接入哪些平台的 API 或爬取哪些网站？
- [ ] 时区处理库的选择：Moment.js (已过时) vs Day.js vs Luxon？
- [ ] 是否需要支持用户登录以保存“我的日历”？（MVP 建议先用 LocalStorage）。

---
**文档版本**：v1.1  
**更新日期**：2026-01-22  
**状态**：进行中
